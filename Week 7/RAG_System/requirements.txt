# --- Core (required, fully offline) ---
scikit-learn>=1.3
numpy>=1.24
faiss-cpu>=1.7        # vector store. Falls back automatically to NumPy brute-force search if not installed.
PyMuPDF>=1.24          # PDF text extraction
python-docx>=1.1       # .docx ingestion

# --- Optional, for higher-quality online backends ---
# Uncomment to enable `--embedder sentence-transformers`
# sentence-transformers>=3.0

# Uncomment to enable `--generator hf-local` (e.g. google/flan-t5-base)
# transformers>=4.40
# torch>=2.2

# Uncomment to enable `--generator openai`
# openai>=1.30
